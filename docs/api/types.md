# Types API

Type definitions and data structures used throughout AntFlow.

## Overview

The `antflow.types` module defines the core data structures used for type hinting and data exchange between components, such as `TaskEvent`, `WorkerMetrics`, and `PipelineStats`.

## Type Reference

::: antflow.types
    options:
      show_root_heading: false
      show_source: false
      members_order: source
